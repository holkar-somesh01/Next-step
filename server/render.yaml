services:
  - type: web
    name: next-step-chat-api
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: ~18.0.0
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false
